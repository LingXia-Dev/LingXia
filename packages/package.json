{
  "name": "@lingxia/workspace",
  "version": "0.0.0",
  "private": true,
  "description": "LingXia in-repo library workspace: links and builds the @lingxia/* packages for local development. Living under packages/ (not the repo root) keeps the example apps in examples/ as standalone installs, untouched by this workspace.",
  "workspaces": [
    "*"
  ]
}
