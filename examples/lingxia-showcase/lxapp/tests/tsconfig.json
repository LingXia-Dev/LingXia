{
  "extends": "../tsconfig.base.json",
  "compilerOptions": {
    "lib": ["ES2020"],
    "types": [],
    // The suite pulls its backlog registry from a plain `.mjs` module.
    "allowJs": true,
    "checkJs": false
  },
  "include": ["**/*.ts", "globals.d.ts"],
  "exclude": []
}
