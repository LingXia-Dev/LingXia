[package]
name = "{{WINDOWS_CRATE_NAME}}"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "{{WINDOWS_EXECUTABLE_NAME}}"
path = "src/main.rs"

[features]
default = ["standard"]
standard = ["host/standard", "lingxia-windows/standard"]
shell-runtime = ["host/shell-runtime", "lingxia-windows/shell-runtime"]
terminal-runtime = ["host/terminal-runtime", "lingxia-windows/terminal-runtime"]
devtools = ["host/devtools"]
webview-input = ["host/webview-input", "lingxia-windows/webview-input"]

[dependencies]
lingxia-windows = { version = "{{LINGXIA_VERSION}}", default-features = false }
host = { package = "{{HOST_CRATE_NAME}}", path = "../{{HOST_CRATE_NAME}}", default-features = false }

[build-dependencies]
lingxia-windows-build = { version = "{{LINGXIA_VERSION}}" }
