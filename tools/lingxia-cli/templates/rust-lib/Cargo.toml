[package]
name = "{{project_name}}"
version = "0.1.0"
edition = "2024"
description = "Native library for LingXia App (builds to .so/.a)"

[lib]
name = "lingxia"
# cdylib for Android/Harmony (.so), staticlib for iOS (.a)
crate-type = ["cdylib", "staticlib"]

[dependencies]
lingxia = "{{CRATE_VERSION}}"
