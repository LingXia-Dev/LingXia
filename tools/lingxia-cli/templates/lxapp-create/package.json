{
  "name": "{{APP_PACKAGE_NAME}}",
  "version": "0.1.0",
  "description": "A LingXia mini app powered by {{FRAMEWORK_DISPLAY}}",
  "type": "module",
  "scripts": {
    "dev": "lingxia build",
    "build": "lingxia build --release",
    "release": "lingxia build --release --package"
  },
  "keywords": ["lingxia", "miniapp", "{{FRAMEWORK}}"],
  "author": "",
  "license": "MIT",
  "lingxia": {
    "framework": "{{FRAMEWORK}}"
  },
  "dependencies": {
    "{{FRAMEWORK_PKG}}": "^{{LINGXIA_BRIDGE_VERSION}}",
    "@lingxia/rong": "^{{RONG_VERSION}}",
    "@lingxia/types": "^{{LINGXIA_TYPES_VERSION}}",
    {{FRAMEWORK_RUNTIME_DEPS}}
  },
  "devDependencies": {
    {{FRAMEWORK_DEV_DEPS_PREFIX}}"typescript": "^5.9.3"
  }
}
