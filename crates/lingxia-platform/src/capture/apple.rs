//! Apple capture types. Compile-only until the Swift bridge can start a
//! real session. An empty provider set is the composition error until
//! then — this module is not an always-unsupported placeholder.
