/**
 * Create new LxApp project
 */

/**
 * Create command entry point
 */
export async function createCommand(appName: string): Promise<void> {
  console.log(`💡 This feature will be implemented in the future.`);
  console.log(`📝 App name: ${appName}`);
}
