{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "LingXia Native-Scoped Locale Schema",
  "description": "Recursive schema for single-audience locale documents (i18n/logic/*.yaml, i18n/android/*.yaml, i18n/apple/*.yaml, i18n/harmony/*.yaml). Only plain string leaves are allowed — single-audience keys have no need for the `default/android/apple/...` variant override.",
  "type": "object",
  "minProperties": 1,
  "additionalProperties": {
    "$ref": "#/definitions/node"
  },
  "definitions": {
    "node": {
      "anyOf": [
        {
          "type": "string",
          "minLength": 1
        },
        {
          "type": "object",
          "minProperties": 1,
          "additionalProperties": {
            "$ref": "#/definitions/node"
          }
        }
      ]
    }
  }
}
